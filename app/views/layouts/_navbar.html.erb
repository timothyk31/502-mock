<nav class="bg-gray-800 p-4">
  <div class="container mx-auto flex justify-between items-center">
    <div class="text-white text-lg font-bold">
      <%= link_to "ClearApp", root_path %>
    </div>
    <div>
      <ul class="flex space-x-4">
          <li>
            <% if current_member && current_member.admin? %> 
              <%= link_to "Dashboard", admin_path, class: "text-white" %>
            <% else %>
              <%= link_to "Dashboard", members_path, class: "text-white" %>
            <% end %>
          </li>
          
          <% if current_member && current_member.admin? %> 
            <li>
              <%= link_to "Members", list_members_path, class: "text-white" %>
            </li>
            <li>
              <%= link_to "Events", events_path, class: "text-white" %>
            </li>
          <% end %>
          <li>
            <%= link_to 'Logout', destroy_member_session_path, method: :delete, class: 'btn btn-primary bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded' %>          </li>
      </ul>
    </div>
  </div>
</nav>